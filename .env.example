# Copy this file to .env.local and fill in your values.
# .env.local is gitignored and will never be committed.

# MongoDB connection string
# Development: mongodb://localhost:27017/notecode  (local Docker — see docker-compose.yml)
# Production:  mongodb+srv://<user>:<password>@<cluster>.mongodb.net/notecode
MONGODB_URI=
